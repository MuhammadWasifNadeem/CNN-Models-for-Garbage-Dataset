# CNN-Models-for-Garbage-Dataset

Classifies waste into 6 categories using custom CNNs trained from scratch.

## Models
- Deeper Custom CNN: ~55% validation accuracy (recommended)
- Simpler CNN: ~21% validation accuracy (baseline)

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
